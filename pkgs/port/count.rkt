#lang racket/base
(require "input-port.rkt")

(provide port-count!
         port-count-byte!)

(define (port-count! orig-in v bstr start)
  (void))
  
(define (port-count-byte! orig-in v)
  (void))


  
